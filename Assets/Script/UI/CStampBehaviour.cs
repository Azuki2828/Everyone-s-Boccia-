using System.Collections;
using System.Collections.Generic;
using UnityEngine;

enum EnStampTypes
{
    enGood,
    enCry,
    enOsii,
    enThingking,
    enMitei
}

public class CStampBehaviour : MonoBehaviour
{
    [SerializeField]
    EnStampTypes m_myType;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
